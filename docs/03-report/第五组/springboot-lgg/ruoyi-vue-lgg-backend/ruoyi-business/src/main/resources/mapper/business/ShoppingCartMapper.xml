<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.business.mapper.ShoppingCartMapper">

    <select id="list" resultType="com.ruoyi.business.entity.ShoppingCart">
        select id,
               name,
               image,
               user_id,
               fruit_id as dish_id,
               fruit_box_id as setmeal_id,
               fruit_flavor as dish_flavor,
               number,
               amount,
               create_time
        from lgg_shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="setmealId != null">
                and fruit_box_id = #{setmealId}
            </if>
            <if test="dishId != null">
                and fruit_id = #{dishId}
            </if>
            <if test="dishFlavor != null">
                and fruit_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

    <insert id="insertBatch" parameterType="list">
        insert into lgg_shopping_cart (name, image, user_id, fruit_id, fruit_box_id, fruit_flavor, number, amount, create_time)
        values
        <foreach collection="shoppingCartList" item="sc" separator=",">
            (#{sc.name},#{sc.image},#{sc.userId},#{sc.dishId},#{sc.setmealId},#{sc.dishFlavor},#{sc.number},#{sc.amount},#{sc.createTime})
        </foreach>
    </insert>
</mapper>
