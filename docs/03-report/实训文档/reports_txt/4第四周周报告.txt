CHANGZHOU INSTITUTE OF TECHNOLOGY
专业方向课程设计
周报告

学院名称：计算机信息工程学院
专业名称：软件工程
班    级：23软件一
学    号：23030301
姓    名：曹磊
学校导师：蒋巍

实训时间：2026年6月15日 - 2026年7月10日
第四周周报告

日期：2026年7月6日 - 2026年7月10日

工作情况记录：
1. 集成 RabbitMQ 分布式消息中间件以实现微服务架构下高并发支付成功事件的**异步解耦**。为防止支付微服务 `lgg-pay` 在确认支付后，由于同步向后续各种非核心系统（如统计服务、通知推送服务）发送同步请求而导致的接口响应过慢和线程阻塞，我们引入了消息队列进行流量削峰。在 RabbitMQ 中，我们声明了一个类型为 `Topic` 的直连交换机 `pay.exchange`，并将专门处理包装通知的消息队列 `pay.success.queue` 通过 Routing Key `pay.success` 绑定到该交换机上。当模拟支付服务处理扣款成功后，首先向业务系统发送同步的 Feign 更新状态请求，随后通过 `RabbitTemplate` 将包含了订单号的事件消息异步投递到交换机中。通知微服务 `lgg-notice` 中的监听器 `PaySuccessConsumer` 采用手动 Consumer Acknowledge（ACK）模式监听消费，当业务处理完毕后显式向 Broker 发送确认信号，若消费端抛出异常则通过 NACK 使消息重新入队，确保了消息投递的 100% 可靠性。
2. 实现基于双独立信道的 WebSocket 实时长连接消息广播。为了让管理后台的商户老板和后勤仓库包装员能分别接收到针对性强、不相互干扰的即时提醒，系统设计了两个独立的 WebSocket 端点：第一套是运行在 `lgg-business` 上的 `/ws/{sid}` 端点，主要面向商户前台，当收到模拟支付回调时，服务端主动向连接的所有 Web 前端客户端推送“来单提醒语音”和催单广播；第二套是运行在 `lgg-notice` 上的 `/websocket/{userId}` 端点，主要面向仓库包装员，当监听类 `PaySuccessConsumer` 异步监听到支付成功的 RabbitMQ 消息后，迅速包装消息体并通过该 WebSocket 管道向对应的包装终端弹窗播报“请及时打包包装”。这种双通道、精细化路由的设计不仅避免了不同岗位信息过载，也降低了单点长连接服务器的线程负载。
3. 构建生产级部署与自动化集成质量保障体系。引入 PM2 进程管理器对全系统的服务进程进行托管，编写了 `ecosystem.config.cjs` 配置文件，为 Gateway 网关、后台核心业务、模拟支付及通知推送等 5 个 Java jar 应用统一配置了启动入口、最大内存限制与自动故障重启策略。在质量保障方面，编写并运行了 Newman 自动化接口集成测试，验证核心 REST API 的状态码与响应体格式；同时利用 Playwright 编写了 E2E 自动化端到端测试脚本，在无头浏览器模式下全自动跑通了“用户登录小程序、水果加购、地址自动填充、下单模拟支付、商家管理端 WebSocket 来单播报、商户接单、仓库弹窗包装、配送与用户确认收货”的完整闭环，极大地降低了现场演示的崩溃风险。

工作、学习体会及收获：
本周是本学期专业方向课程设计的最后收官阶段，涉及了分布式消息机制解耦、实时推送以及自动化测试交付等多项工程化实践。通过使用 RabbitMQ 消息队列，我深刻领会到了异步解耦与削峰填谷对分布式系统高可用、高扩展性的核心意义。在深入设计手动 ACK 机制和消息、队列、交换机三层持久化配置的过程中，我掌握了如何防止分布式环境下消息意外丢失的方法。而在构建双独立 WebSocket 信道时，我不仅深入理解了 WebSocket 握手升级以及保持心跳连接的底层网络原理，更认识到在业务大系统中必须按用户角色、职责对长连接管道进行精细化路由划分。最后，PM2 长守护以及自动化测试（Newman & Playwright）的落地，让我真正建立起了软件工程“持续交付与自动化验证”的质量防护网。这不仅确保了系统在演示现场离线环境下的极其稳定运行，更让我真正将课本上的微服务理论转化为工程落地实践，收获了宝贵的实战经验。
