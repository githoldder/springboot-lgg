CHANGZHOU INSTITUTE OF TECHNOLOGY
专业方向课程设计
周报告

学院名称：计算机信息工程学院
专业名称：软件工程
班    级：23软件一
学    号：23030301
姓    名：曹磊
学校导师：蒋巍

实训时间：2026年6月15日 - 2026年7月10日
第一周周报告

日期：2026年6月15日 - 2026年6月19日

工作情况记录：
1. 开展水果生鲜运营平台项目的需求分析与可行性研究。针对生鲜水果在线零售与配送的具体商业场景，深度梳理出管理后台与微信小程序客户端的完整业务闭环。管理后台的核心功能被细化为“水果分类管理”（包括分类的新增、编辑、排序以及状态变更）、“水果品种维护”（支持单品信息编辑、口感口味规格配置、起售停售拦截）、“订单处理与状态流转”（涵盖待付款、待接单、派送中、已完成等状态的手动与自动变更）以及“运营数据可视化大屏”（展示今日销售额、订单量、新增用户以及畅销商品排名）。微信小程序客户端则规划为 C 端用户的消费入口，主要用例包括“水果分类与商品列表的动态浏览”、“商品多口味规格选择”、“购物车数据的本地与云端同步加购”、“收货地址簿的增删改查”以及“订单确认、拉起微信支付与配送状态追踪”。通过对核心业务的梳理，我们构建了系统的顶层用例模型，消除功能边界上的歧义，明确了超级管理员、普通运营员以及普通用户之间的职责隔离。
2. 针对分布式微服务高可用特性，规划并设计了系统的多服务解耦架构。为了避免单体应用的单点故障和高并发下的性能瓶颈，本系统在逻辑与物理上被拆分为五个核心微服务模块：统一 API 路由网关服务 (lgg-gateway)，运行于 8090 端口，作为全平台统一的流量入口，负责动态路由分发与跨域处理；生鲜核心业务服务 (lgg-business)，运行于 8088 端口，承载了最密集的分类、商品与订单持久化逻辑；微信模拟扣款与状态更新服务 (lgg-pay)，运行于 8085 端口，专门处理外部模拟回调动作，并通过 OpenFeign 同步调用业务服务；RabbitMQ 事件消费与实时消息推送服务 (lgg-notice)，运行于 8086 端口，作为消息通知的末端节点，异步消费支付事件并通过 WebSocket 推送提醒；若依管理鉴权基座服务 (lgg-admin)，运行于 8080 端口，负责系统管理员的登录认证与权限控制。各微服务之间各司其职，保证了高内聚与低耦合。
3. 设计水果生鲜系统底层的 MySQL 关系型数据模型。在明确系统顶层用例后，我们设计了 10 张核心业务表并绘制了系统的 E-R 关系图。核心表包括：`lgg_category`（存储水果分类及显示顺序）、`lgg_fruit`（存储单品水果的名称、价格、图片和状态）、`lgg_fruit_flavor`（存储单品水果的口感与规格属性，如甜度、分量）、`lgg_fruit_box`（存储精选果篮/套餐组合）、`lgg_fruit_box_item`（存储果篮中包含的单品水果及份数）、`lgg_shopping_cart`（暂存 C 端用户的购物车加购数据）、`lgg_orders`（存储订单主表、订单号、支付状态及配送信息）、`lgg_order_detail`（存储订单下挂载的水果单品明细，包含单价和数量）、`lgg_address_book`（存储用户收货地址簿）以及 `lgg_user`（存储微信 C 端注册用户的 openid 和个人资料）。在物理设计上，为了保障读写性能，我们在 `lgg_category` 的 `category_name`、`lgg_fruit` 的 `fruit_name` 以及 `lgg_orders` 的 `order_no` 上设计了唯一索引，以防止脏数据注入；在商品名称上建立了全文索引以支持高效的模糊检索；在订单明细表的 `order_id` 上建立了普通索引以加速订单详情的关联查询。同时，系统在物理层面去除了所有跨表物理外键约束，转而在 Java 应用层代码中通过业务逻辑保证引用的完整性，从而降低了高并发写入时的数据库锁竞争。

工作、学习体会及收获：
在本周的设计工作中，我深刻体会到了在分布式系统开发中“需求先行、架构先行”的重要性。通过对分类、单品与果篮套餐数据之间一对多和多对多关联模型的梳理，我掌握了如何在不使用物理外键强约束的前提下，利用应用层逻辑保障数据一致性的分布式设计思想。物理外键虽然能在数据库层面保证引用完整性，但在高并发场景下会导致频繁的行锁甚至表锁，并且不利于后续微服务的数据库物理拆分。因此，在业务逻辑层（如删除分类前先查询商品表是否引用）来确保完整性，是分布式系统更为主流的设计选择。此外，通过微服务边界的合理划分，我理解了将流量入口、高频读写服务与低频鉴权、通知服务物理隔离，对于保障整个生鲜运营系统整体高可用性的核心价值。网关 lgg-gateway 能够屏蔽后端微服务的物理 IP 和端口细节，不仅提高了系统的安全性，也为后续服务的横向弹性扩容打下了坚实的基础。通过绘制 E-R 图和模块依赖图，我的系统设计思维得到了极大的锻炼，也为下周的代码实现指明了清晰的方向。
